# ./travis.yml for MSAL android
language: android
jdk: oraclejdk8

# Don't use the Travis Container-Based Infrastructure
sudo: true

# Build Variables
env:
  global:
    - BUILD_TOOLS_VERSION=29.0.3
    - COMPILE_SDK_VERSION=29
    - EMULATOR_API_VERSION=29

    # This is to guarantee a clean gradle log
    - TERM=dumb

android:
  components:
    - extra-google-m2repository

before_install:
  - chmod +x gradlew
  - touch ~/.android/repositories.cfg
  - yes | sdkmanager "tools"
  - yes | sdkmanager "platforms;android-29" | grep -v = || true
  - yes | sdkmanager "build-tools;29.0.3" | grep -v = || true
  - yes | sdkmanager "system-images;android-29;google_apis;x86" | grep -v = || true

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.android/build-cache

before_script:
  - no | avdmanager create avd -n test -k "system-images;android-29;google_apis;x86"
  - emulator -avd test -no-audio -no-window &
  - android-wait-for-emulator
  - adb shell input keyevent 82 &

script:
  - cd $PWD
  - travis_wait ./gradlew clean msal:assembleLocal msal:connectedLocalDebugAndroidTest -PdisablePreDex
