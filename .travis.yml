sudo: required

language: minimal

services:
  - docker

before_install:
- docker pull authclient/ac-28:0.1.1

script:
- docker run --rm -v $TRAVIS_BUILD_DIR -w /home/gradle/project authclient/ac-28:0.1.1 /bin/sh -c "/usr/local/android-sdk/emulator/emulator -avd test -no-audio -no-window -gpu off & /usr/local/android-sdk/platform-tools/adb wait-for-any-device; gradle msal:connectedLocalDebugAndroidTest"